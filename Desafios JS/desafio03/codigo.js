function clicar(){
    let txt = prompt('Digite um número inteiro qualquer')
    let num = Number(txt)
    let res = alert(`Antes de ${num}, temos o número ${num-1}.                                                          Depois de ${num}, temos o número ${num+1}.`)
    
}